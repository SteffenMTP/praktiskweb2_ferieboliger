export const metadata = {
    title: 'List',
    description: 'Show list',
}

export default function show({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>

                
            {children}    
            

        </>
    )
}